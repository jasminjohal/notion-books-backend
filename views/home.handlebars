<div class="container-fluid">
  <h1>TBR</h1>
  {{#each data}}
    <div class="card">
      <img src={{bookCover}} class="card-img" alt="book cover for {{title}}" />
      <div class="card-body">
        <p class="card-text">{{title}} by {{author}}</p>
      </div>
    </div>
  {{/each}}
</div>